#include "main.h"

/**
 * _isdigit - function that verifies if a character is a digit or not
 * @a: tested character
 * Return: returns 1 if upper case, if not 0
 */

int _isdigit(int a)
{
	if ((a >= 48) && (a <= 57)
			return (1);

			return (0);
			}
